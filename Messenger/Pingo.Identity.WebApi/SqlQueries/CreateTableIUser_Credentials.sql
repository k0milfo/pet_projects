CREATE TABLE IF NOT EXISTS "UserCredentials" (user_id UUID PRIMARY KEY, Email TEXT NOT NULL, HashPassword TEXT NOT NULL, Salt TEXT NOT NULL, FOREIGN KEY (user_id) REFERENCES "User"(id) ON DELETE CASCADE);
