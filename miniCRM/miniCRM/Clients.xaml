<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:miniCRM"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" x:Class="miniCRM.Window_Clients"
        mc:Ignorable="d"
        Title="Clients" Height="300" Width="500">
    <Grid Background="#B0F26D" Margin="0,0,0,0">
        <Button x:Name="add_client" Content="Добавить" HorizontalAlignment="Left" Margin="10,20,0,0" VerticalAlignment="Top" Width="100" Height="28"/>
        <DataGrid x:Name="clients_table" d:ItemsSource="{d:SampleData ItemCount=3}" ItemsSource="{Binding YourDataCollection}" Margin="132,10,17,21" AutoGenerateColumns="False" CanUserAddRows="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Компания" Binding="{Binding Company}" />
                <DataGridTextColumn Header="Имя владельца" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Номер телефона" Binding="{Binding Phone_number}" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
